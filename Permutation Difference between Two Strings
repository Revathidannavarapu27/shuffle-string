class Solution:
    def findPermutationDifference(self, s, t):
        difference = 0
        for i in range(len(s)):
            j = t.index(s[i])
            difference += abs(i - j)
        return difference
